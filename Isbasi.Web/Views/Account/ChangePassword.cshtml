@{
    ViewData["Title"] = "Parola Değiştir";
}

<div class="page-header">
    <h1 class="page-title">Parola Değiştir</h1>
</div>

<div class="portlet" style="max-width:520px">
    @if (ViewBag.Error != null)
    {
        <div class="alert alert-danger py-2"><i class="fas fa-exclamation-circle me-1"></i>@ViewBag.Error</div>
    }
    <form method="post" action="/account/changepassword">
        @Html.AntiForgeryToken()
        <div class="mb-3">
            <label class="form-label">Mevcut Parola</label>
            <input type="password" name="currentPassword" class="form-control" required autofocus>
        </div>
        <div class="mb-3">
            <label class="form-label">Yeni Parola</label>
            <input type="password" name="newPassword" class="form-control" minlength="8" required>
            <div class="form-text">En az 8 karakter; en az bir harf ve bir rakam içermelidir.</div>
        </div>
        <div class="mb-4">
            <label class="form-label">Yeni Parola (Tekrar)</label>
            <input type="password" name="newPasswordConfirm" class="form-control" minlength="8" required>
        </div>
        <button type="submit" class="btn btn-save"><i class="fas fa-key me-1"></i> Parolayı Değiştir</button>
    </form>
</div>
